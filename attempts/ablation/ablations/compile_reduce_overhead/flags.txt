# Ablation: compile_reduce_overhead
# Date: 2026-03-04 18:51:51
# NPROC: 1
# ABLATION_DEPTH: 12
# EXTRA_FLAGS: --compile-mode reduce-overhead

torchrun --standalone --nproc_per_node=1 -m scripts.base_train -- \
    --depth=12 \
    --run=dummy \
    --model-tag=ablation_compile_reduce_overhead \
    --compile-mode=default \
    --target-param-data-ratio=2.0 \
    --core-metric-every=999999 \
    --sample-every=-1 \
    --save-every=-1 \
    --compile-mode \
    reduce-overhead \

